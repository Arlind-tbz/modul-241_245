name: Deploy solution-pitch to Cloudflare Pages

on:
  push:
    branches:
      - main
    paths:
      - 'solution-pitch/**'
      - '.github/workflows/deploy-solution-pitch.yml'

jobs:
  trigger-deploy:
    name: Trigger Cloudflare Pages Deploy Hook
    runs-on: ubuntu-latest
    steps:
      - name: Trigger deploy hook
        run: |
          curl -X POST "${{ secrets.CF_PAGES_WEBHOOK_SOLUTION_PITCH }}"
